{
  "name": "@nodebraid/examples",
  "private": true,
  "type": "module",
  "dependencies": {
    "@base-ui/react": "^1.7.0",
    "@fontsource-variable/geist": "^5.3.0",
    "@nodebraid/core": "workspace:*",
    "@nodebraid/renderer-svg": "workspace:*",
    "@tanstack/react-router": "^1.170.32",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.34.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "shadcn": "^4.19.0",
    "tailwind-merge": "^3.6.0",
    "tw-animate-css": "^1.4.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@tanstack/router-plugin": "^1.168.35",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "@vitejs/plugin-react": "^6.1.0",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.2"
  },
  "scripts": {
    "build": "bun run build:dependencies && vite build",
    "build:dependencies": "bun run --filter '@nodebraid/core' build && bun run --filter '@nodebraid/renderer-svg' build",
    "check": "bun run build && tsgo --noEmit -p tsconfig.json && bun run test:browser",
    "dev": "bun run build:dependencies && vite --host 127.0.0.1",
    "test": "bun run build && bun run test:browser",
    "test:browser": "bun run ./tests/basic-svg.browser.ts",
    "typecheck": "bun run build:dependencies && tsgo --noEmit -p tsconfig.json"
  }
}
