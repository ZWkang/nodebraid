# Changesets

This directory contains release metadata managed by [Changesets](https://github.com/changesets/changesets).

Create a changeset for a publishable package change with:

```bash
bun run changeset
```
