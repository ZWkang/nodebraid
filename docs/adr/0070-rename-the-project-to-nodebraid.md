# Rename the project to NodeBraid

The project adopts **NodeBraid** as its public brand and `nodebraid` as its machine identifier because the former CFlow name and `@cflow/*` package namespace are already used by unrelated projects. Since this project's packages have not been published, the repository makes one complete breaking rename across package names, public APIs, protocol strings, documentation, and integration seams without retaining compatibility aliases.
