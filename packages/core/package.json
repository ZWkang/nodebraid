{
  "name": "@cflow/core",
  "type": "module",
  "version": "0.0.1",
  "description": "Public CFlow facade for Kernel and Runtime Plugin APIs.",
  "publishConfig": {
    "access": "public"
  },
  "author": {
    "name": "zwkang",
    "email": "kang95630@gmail.com"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ZWkang/cflow.git",
    "directory": "packages/core"
  },
  "bugs": "https://github.com/ZWkang/cflow/issues",
  "homepage": "https://github.com/ZWkang/cflow/tree/main/packages/core#readme",
  "keywords": [
    "cflow",
    "core",
    "graph-kernel",
    "plugin-runtime"
  ],
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsgo -p tsconfig.build.json --watch",
    "build": "bun run build:dependencies && bun run build:self",
    "build:dependencies": "bun run --filter '@cflow/diagnostics' build && bun run --filter '@cflow/kernel' build && bun run --filter '@cflow/session-api' build && bun run --filter '@cflow/interaction-api' build && bun run --filter '@cflow/renderer-api' build && bun run --filter '@cflow/runtime-cordis' build && bun run --filter '@cflow/plugin-command' build && bun run --filter '@cflow/plugin-kernel' build && bun run --filter '@cflow/plugin-session' build && bun run --filter '@cflow/plugin-renderer' build && bun run --filter '@cflow/plugin-interaction' build && bun run --filter '@cflow/plugin-history' build && bun run --filter '@cflow/layout-api' build && bun run --filter '@cflow/layout-dagre' build && bun run --filter '@cflow/layout-elk' build && bun run --filter '@cflow/plugin-layout' build",
    "build:self": "tsgo -p tsconfig.build.json && bun run test:artifacts",
    "prepack": "bun run build",
    "test": "bun test ./tests",
    "test:artifacts": "bun run ./artifacts/check-declarations.ts",
    "typecheck": "bun run build:dependencies && tsgo --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@cflow/diagnostics": "workspace:*",
    "@cflow/interaction-api": "workspace:*",
    "@cflow/kernel": "workspace:*",
    "@cflow/layout-api": "workspace:*",
    "@cflow/plugin-command": "workspace:*",
    "@cflow/plugin-history": "workspace:*",
    "@cflow/plugin-interaction": "workspace:*",
    "@cflow/plugin-kernel": "workspace:*",
    "@cflow/plugin-renderer": "workspace:*",
    "@cflow/plugin-session": "workspace:*",
    "@cflow/plugin-layout": "workspace:*",
    "@cflow/renderer-api": "workspace:*",
    "@cflow/runtime-cordis": "workspace:*",
    "@cflow/session-api": "workspace:*"
  }
}
